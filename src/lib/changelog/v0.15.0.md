---
version: 0.15.0
date: 2026-03-24
title: Flexible Scan Modes & Credential Enhancements  
social_post: • Light scan mode for faster, minimal scans\n• Remote Docker container scanning\n• Credential management within discovery modal\n• Loopback and Docker scan reliability improvements
contributors: Maya,Nev,dependabot[bot],rkoktan
category: B
email_subject: Scan faster and manage credentials—new Scanopy release
---

This release focuses on making network scanning more flexible and reliable, with powerful new scan modes and improved credential management.

## New Features

- Add light scan mode, enabling you to run faster scans with minimal host probing when detailed discovery isn't required ([#536](https://github.com/scanopy/scanopy/pull/536)).
- Introduce remote Docker container scanning, letting you discover containers on hosts across your network without needing local access ([#538](https://github.com/scanopy/scanopy/pull/538)).
- Enable credential management directly within the discovery modal, allowing you to quickly assign and edit credentials right where you launch scans ([#543](https://github.com/scanopy/scanopy/pull/543)).
- Support full_scan_interval=0 so you can disable scheduled deep scans when they're not needed ([#544](https://github.com/scanopy/scanopy/pull/544)).
- Add explanatory DocHints throughout credential, discovery, and network screens to clarify scan modes, credential mapping, and recommended actions.

  ![Credential assignment and DocHints in discovery modal](/changelog/v0.15.0/discovery-modal-credential-management.png)
  
- Add loopback interface support, ensuring subnets and hosts connected via loopback are accurately discovered and visualized ([#552](https://github.com/scanopy/scanopy/pull/552)).

## Improvements

- Polish credential wizard and existing credential UX, making it easier to assign, update, and map credentials across tabs and network screens ([#553](https://github.com/scanopy/scanopy/pull/553)).
- Move Scan Info to the Details tab, present scan history and last run data more clearly, and simplify the Scan Info card when no scans are completed.
- Improve Docker discovery reliability with smart endpoint probing and robust credential passthrough, reducing errors for proxy and bridge container setups.
- Prefer host IP over loopback for Docker socket binding to avoid connectivity issues during network scans.
- Clarify scan mode explanations and add scan mode visibility in scan history views so you understand what type of scan ran and when.
- Always show credential-related tabs and remove confusing disabled "delete" buttons for a cleaner interface.

## Bug Fixes

- Fix credentials sometimes using the wrong target field during creation and mapping, ensuring accurate assignment for all scan types.
- Fix host deduplication logic so loopback and SNMP hosts are properly shown without duplicates.
- Fix Docker connection retries and dropped host-mode containers, improving scan coverage for complex Docker setups.
- Exclude unresponsive or non-interfaced hosts from discovered counts for more accurate scan statistics.
- Make Docker Proxy port field optional with a sensible default, avoiding unnecessary configuration errors.
- Block daemon deletion if it has active sessions running, preventing accidental loss of scan progress.
- Fix stalled session cleanup for deleted daemons so Scanopy doesn't hang after removing servers.

---

**Full Changelog**: https://github.com/scanopy/scanopy/compare/v0.14.19...v0.15.0
